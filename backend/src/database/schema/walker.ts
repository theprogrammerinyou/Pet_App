export const walker = {
    name: String,
    gender: String,
    age: Number,
    price: Number,
    rating: Number,
    reviews: Number,
    location: String,
    about: String,
    timings: String,
    appointmentBooked: Boolean
}